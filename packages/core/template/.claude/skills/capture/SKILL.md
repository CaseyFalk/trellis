---
name: capture
description: File settled understanding from this session into the knowledge base — routed to each concept's home, with provenance.
disable-model-invocation: true
argument-hint: "what to capture (optional; defaults to everything settled this session)"
---

Thin Claude Code wrapper. The procedure is portable and agent-agnostic — follow
[`docs/capture.md`](../../../docs/capture.md), which carries the full capture
steps. Read `AGENTS.md` and `docs/conventions.md` first for the conventions it
relies on.
